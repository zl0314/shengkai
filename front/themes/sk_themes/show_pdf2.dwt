<!DOCTYPE html>
<html lang="zh">
<head>
	<title>首页</title>
	<meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
	<meta name="viewport" content="width=device-width, initial-scale=1, minimum-scale=1, maximum-scale=1, user-scalable=no">
	<meta name="apple-mobile-web-app-capable" content="yes">
	<meta name="apple-mobile-web-app-status-bar-style" content="black" />
	<meta name="format-detection" content="telephone=no">
	<meta http-equiv=”X-UA-Compatible” content=”IE=edge,chrome=1″/>
	<style>
		body{
			margin:0;
			padding:0;
			font-size: 12px;
			font-family: "微软雅黑"
		}
		.logo{
			width:150px;
			margin:30px auto;
		}
		.logo img{
			width: 100%;
		}
		table{
			table-layout: fixed;
			margin:0 auto;
			border:1px solid #000000;
			border-collapse:none;
			border-spacing: 0;
			border-bottom:0;
		}
		tr{
			border-bottom: 1px solid #000000;
		}
		th,td{
			border-right:1px solid #000000;
			text-align: center;
			border-bottom:1px solid #000000;
			border-spacing: 0;
			height:30px;
			vertical-align: middle;
			word-wrap:break-word;
			word-break:break-all;
		}
		th:last-child,td:last-child{
			border-right: none;
		}
	</style>
</head>
<body>
<div class="logo">
	<img src="images/logo22.png">
</div>
<table>
	<tr>
		<th width="20%;">订单号</th>
		<th width="30%;">订单来源（代理商名字/个人）</th>
		<th width="10%;">金额</th>
		<th width="15%;">订单状态</th>
		<th width="25%;" colspan="2">订单时间</th>
	</tr>
	<tr>
		<td>{$order_info.order_sn}</td>
		<td>{if $order_info.third_platform == 1}飞猪{elseif $third_platform == 2}携程{else}个人{/if}</td>
		<!-- <td>￥{$order_info.goods_amount}</td> -->
		<td>￥{if $order_info.third_platform == 1}{$order_info.order_amount}{else}{$order_info.goods_amount}{/if}</td>
		<td>
		    <!--{if $order_info.shipping_status == 3}-->
                已取票
            <!--{else}-->
                {$order_info.order_status}
            <!--{/if}-->
		</td>
		<td colspan="3">{$order_info.add_time}</td>
	</tr>
	<tr>
		<th colspan="6">票务信息</th>
	</tr>
	<tr>
		<th>场次名称</th>
		<th>时间</th>
		<th colspan="2">地点</th>
		<th colspan="2">数量</th>
	</tr>
	<!-- {foreach from=$info_list item=info } -->
	<tr>
		<td>{$info.game_name}&nbsp;{$info.num_name}</td>
		<td>{$info.num_start}</td>
		<td colspan="2">{$info.region_name}&nbsp;{$info.pitch_name}</td>
		<td colspan="2">{$info.order_goods_number}张</td>
	</tr>
	<!-- {/foreach } -->
	{foreach from=$combo_info_list item=combo_info}
	<tr>
        <td>{$combo_info.game_name}&nbsp;{$combo_info.num_name}(套餐)</td>
        <td>{$combo_info.num_start}</td>
        <td colspan="2">{$combo_info.region_name}&nbsp;{$combo_info.pitch_name}</td>
        <td colspan="2">{$combo_info.order_goods_number}张</td>
    </tr>
	{/foreach}
	<tr>
		<th colspan="6">持票人信息</th>
	</tr>
	<tr>
		<th>姓名</th>
		<th>护照号码</th>
		<th>街道门牌号</th>
		<th>电话</th>
		<th>邮箱</th>
        <th  colspan="2">票号</th>
	</tr>
	<!-- {foreach from=$order_bearer item=bearer } -->
	<tr>
		<td>{$bearer.cn_customer_name}</td>
		<td>{$bearer.passport_number}</td>
		<td>{$bearer.cn_name_street}</td>
		<td>{$bearer.mobile}</td>
		<td>{$bearer.mail}</td>
        <td colspan="2">{$bearer.ticket_code}</td>
	</tr>
	<!-- {/foreach} -->
	<tr>
		<th colspan="6">联系人信息</th>
	</tr>
	<tr>
		<th>姓名</th>
		<th>电话</th>
		<th colspan="4">邮箱</th>
	</tr>
	<tr>
		<td>{$lianxiren.consignee}</td>
		<td>{$lianxiren.mobile}</td>
		<td colspan="4" >{$lianxiren.email}</td>
	</tr>

</table>
</body>
</html>